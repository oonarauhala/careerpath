stages:
  - build

build_project:
  stage: build
  script:
    - xcodebuild clean -project Career Path.xcodeproj -scheme SchemeName 
    - xcodebuild test -project Career Path.xcodeproj -scheme SchemeName -destination 'platform=iOS Simulator,name=iPhone 8,OS=13’ 
  tags:
